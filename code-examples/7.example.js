
module.exports.solve = (a, b, c) => {
    let total = a + b;
    total = total + c;

    return total;
}

// solve(1, 2, -3) // we expect 0


// module.exports.solve = (a, b, c) => {
//     const total = a + b + c;
//     return total;
// }